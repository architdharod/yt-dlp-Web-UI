fastapi==0.141.1
uvicorn[standard]==0.52.4
# The `default` extra pulls in yt-dlp-ejs, the JavaScript yt-dlp's YouTube
# extractor runs in the deno runtime the image ships.  Without it yt-dlp would
# have to fetch those scripts at runtime (--remote-components) or go without
# them, which is what "Sign in to confirm you're not a bot" usually means.
yt-dlp[default]>=2026.8.19
mutagen==1.48.1
python-multipart==0.0.32
pydantic==2.13.5
sse-starlette==3.4.10
# Async HTTP client for the Navidrome and Lidarr rescan calls.
httpx==0.28.1

# MusicBrainz text search for the automatic tag fix.  Pinned: 0.7.1 (Jan 2020)
# is the current and only maintained release; the project is in maintenance
# mode, so app/tagger.py wraps it thinly enough to be swapped.
musicbrainzngs==0.7.1

# Read directly, not only through ytmusicapi: app/fetch.py makes the one
# bounded GET that app/spotify.py and app/bandcamp.py read a public page with,
# and app/ytmusic.py catches urllib3's errors by class.  Declared here because
# ytmusicapi's own floor (requests>=2.22) allows urllib3 1.26, whose
# HTTPResponse has no `read1` -- and reading one socket read at a time is what
# lets app/fetch.py check the probe's deadline between chunks.
requests>=2.32
urllib3>=2,<3

# Keyless YouTube Music access for the artist enumeration in app/ytmusic.py: a
# channel URL becomes a discography (albums, EPs, singles) instead of the pile
# of uploads a flat yt-dlp pass sees.  Pinned: it parses YouTube's internal
# JSON, so a release can change what a call returns, and app/ytmusic.py treats
# every parse failure as "not a music artist" and falls back to yt-dlp.
ytmusicapi==1.12.2

# The yt-dlp plugin half of the PO-token sidecar (`pot-provider` in
# docker-compose.yml).  YouTube expects a proof-of-origin token with every
# player request; an anonymous client without one loses formats and is asked to
# "sign in to confirm you're not a bot" within a few dozen downloads.  This
# package is discovered by yt-dlp's plugin loader on import -- there is nothing
# to wire up in code beyond the `youtubepot-bgutilhttp:base_url` extractor arg
# in app/downloader.py, which points it at the sidecar.  Pinned to match the
# image tag in docker-compose.yml, and it needs yt-dlp >= 2025.05.22, well
# below our floor above.
bgutil-ytdlp-pot-provider==1.3.2
